#game-menu
  %a#get-new-word{:href => '#'}
    = t('game.get_new_word')
  %a#post-day-score-to-fb{:href => '#', 'data-user-id' => current_user ? current_user.id : 0}
    = t('game.post_day_score_to_fb')

  = render 'words/new'
