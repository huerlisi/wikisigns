- if controller.class.eql?WordsController
  #logins
    - unless user_signed_in?
      .login-info
        = t('login_information')
      = link_to t('login'), login_path, :id => 'user-login'
    - else
      = link_to t('logout'), logout_path, :id => 'user-logout'
    